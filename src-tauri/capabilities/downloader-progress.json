{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "downloader-progress",
  "description": "Only allow the downloader window to receive its progress and close after launch.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close"
  ]
}
